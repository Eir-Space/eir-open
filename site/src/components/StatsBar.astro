---
const { stats } = Astro.props as {
  stats: Array<{ value: string; label: string }>;
};
---

<div class="stats-bar not-content">
  {stats.map((stat) => (
    <div class="stat-item">
      <span class="stat-value">{stat.value}</span>
      <span class="stat-label">{stat.label}</span>
    </div>
  ))}
</div>
