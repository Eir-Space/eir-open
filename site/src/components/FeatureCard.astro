---
import Card from './Card.astro';
const { title, icon } = Astro.props as { title: string; icon?: string };
---

<Card variant="feature" icon={icon} title={title}>
  <div class="feature-card-desc mt-1.5 text-muted-foreground line-clamp-2">
    <slot />
  </div>
</Card>
