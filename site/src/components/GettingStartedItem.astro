---
import Icon from './Icon';
const { title, icon } = Astro.props as { title: string; icon: string };
---

<div class="getting-started-item">
  <h3 class="getting-started-heading">
    <Icon name={icon} size={16} />
    {title}
  </h3>
  <slot />
</div>
