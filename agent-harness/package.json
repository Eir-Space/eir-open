{
  "name": "@eir-open/agent-harness",
  "private": true,
  "workspaces": [
    "packages/agent-core",
    "packages/skill-kit",
    "packages/health-memory"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "clean": "npm run clean --workspaces"
  },
  "engines": {
    "node": ">=18"
  }
}
